/*
  BOM [Browser Object Model]
  - stop()
  - print()
  - focus()
  - scrollTo(x, y || Options)
  - scroll(x, y || Options)
  - scrollBy(x, y || Options)
*/

//let myNewWindow = window.open("https://elzero.org/javascript-2021-scroll-scrollto-scrollby-focus-print-stop/","","width=500,height=500");

// window.scrollTo(500,500);

// window.scrollBy(600,800);

// window.scroll(200,400);

//faire dans console avec répete chaque un des c'est méthode

//optionnal
window.scrollTo({
    left:500,
    top:200,
    behavior:"smooth"
});

